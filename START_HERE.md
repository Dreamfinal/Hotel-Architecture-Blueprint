# เริ่มที่นี่ — Hotel Architecture Blueprint

ชุดนี้เปลี่ยนงานใหญ่ให้เป็น “โรงแรม” ที่ประกอบด้วยห้องงานขนาดพอดี แต่ละห้องมีขอบเขต ผู้รับผิดชอบ ผลส่งมอบ และวิธีตรวจรับของตัวเอง จึงให้ agent หลายตัวทำงานขนานกันได้โดยไม่แก้ทับกันหรือแบกบริบททั้งโปรเจกต์

## ใช้งานกับ agent ภายในหนึ่งนาที

1. แตก ZIP ไว้ในโฟลเดอร์โปรเจกต์ หรือแนบทั้งชุดให้ agent อ่าน
2. พิมพ์ข้อความนี้:

> ศึกษา Hotel Architecture Blueprint ในโฟลเดอร์นี้ให้ครบ แล้วนำมาออกแบบโรงแรมสำหรับงานที่ฉันกำลังจะให้ โดยยังไม่ลงมือแก้โปรเจกต์จนกว่าฉันจะอนุมัติแผน

3. บอกผลลัพธ์ที่ต้องการ เช่น “ย้ายระบบล็อกอิน”, “แปลข้อความ 5,000 บรรทัด” หรือ “สร้างเว็บไซต์บริษัท”
4. ให้ agent ส่ง `HOTEL_MANIFEST.json` และรายการห้องมาให้ตรวจ ก่อนเปิดโรงแรม

ถ้าต้องการให้ลงมือทันทีและยอมรับการเปลี่ยนแปลง ให้เปลี่ยนท้าย prompt เป็น:

> ตรวจโปรเจกต์ สร้างโรงแรม แล้วดำเนินงานตามห้องได้เลย ภายในขอบเขตนี้: ...

## ลำดับไฟล์ที่ควรอ่าน

1. `BLUEPRINT.md` — แนวคิดและโครงสร้าง
2. `GIT_PLAYBOOK.md` — วิธีออกแบบโปรเจกต์บน Git, branch, worktree, claim และ merge
3. `OPERATING_RULES.md` — กฎความปลอดภัยและ lifecycle
4. `templates/` — ไฟล์ที่คัดลอกไปใช้จริง
5. `examples/website-hotel/` — ตัวอย่างโรงแรมขนาดเล็ก
6. `tools/hotel_validate.py` — validator แบบไม่ใช้ไลบรารีภายนอก

## แก่นที่ต้องจำ

> โมเดลเล็กทำงานยากได้เมื่อปัญหาถูกทำให้มีขอบเขตชัด ส่วนโมเดลที่เก่งกว่าใช้กับการออกแบบห้อง ตัดสินใจข้ามห้อง และแก้ความกำกวมที่มีผลสูง

Blueprint นี้เป็นโครงทั่วไป ไม่ผูกกับ Codex, Claude, GitHub หรือระบบ router ใดระบบหนึ่ง
