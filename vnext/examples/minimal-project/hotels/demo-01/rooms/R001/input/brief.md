# R001 compiled brief

Produce the first bounded demo output. This file represents context compiled by the Hotel architect into the Room packet on the control plane.

The Guest may additionally read `source/input.md` from its claim code base because that path is explicitly listed under `source_read_allowlist`.