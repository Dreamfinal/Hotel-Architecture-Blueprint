# Minimal Project — Project Safety

- `CURRENT_STATE.md` is the sole current-state source.
- Guests may read only Reception + their claimed Room packet/source allowlist.
- No Team Repo access is required.
- Claims/returns use the Hotel vNext fixed claim branch protocol.
- This example is documentation/test data; it does not authorize publish, deploy, or destructive actions.