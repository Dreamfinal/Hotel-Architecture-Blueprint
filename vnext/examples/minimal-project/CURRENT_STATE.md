# Minimal Project — Current State

Phase: demo
Active Hotel: `demo-01`
Hotel lifecycle: `READY_TO_OPEN`

R001 is dependency-ready. R002 depends on R001 and is blocked until R001 is accepted/integrated.

Next action: run Hotel validation, then perform the explicit opening control transition.